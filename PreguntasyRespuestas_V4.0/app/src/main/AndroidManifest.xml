<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.preguntasyrespuestas">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.actividades.inside.JuegoActivity"></activity>
        <activity android:name=".ui.actividades.inside.CambiarImagenActivity" />
        <activity android:name=".ui.actividades.inside.CambiarContraActivity" />
        <activity
            android:name=".ui.actividades.inside.InsideActivity"
            android:label="@string/title_activity_inside"
            android:theme="@style/NoActionBar" />
        <activity android:name="com.example.preguntasyrespuestas.ui.actividades.ingreso.RegistroActivity" />
        <activity android:name="com.example.preguntasyrespuestas.ui.actividades.ingreso.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>